import React from "react";

const Loader: React.FC = () => {
  return (
    <div
      style={{
        textAlign: "center",
        padding: "50px",
        fontSize: "18px",
        fontWeight: "bold",
      }}
    >
      Loading...
    </div>
  );
};

export default Loader;
